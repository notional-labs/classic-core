linters:
  disable-all: true
  enable:
    - errcheck
    - ineffassign
    - unconvert
    - misspell
    - govet
    - unused
    - goconst
    - gosec
    - staticcheck
issues:
  exclude:
    - composite
run:
  tests: false
